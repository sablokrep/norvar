[package]
name = "norvar"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.19.1", features = ["train", "vision", "network", "cpu", "cuda", "ndarray", "wgpu", "dataset", "std", "autodiff", "fusion", "store","metrics"] }
